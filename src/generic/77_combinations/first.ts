// const combine = (n: number, k: number): number[][] => {
//   var array = ["apple", "banana", "lemon", "mango"];

//   var result = array.map((v, i) => array.slice(i + 1).map((w) => [v, w]));

//   return result;
// };

// console.log("combine", combine(4, 2));
