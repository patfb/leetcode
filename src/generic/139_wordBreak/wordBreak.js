/**
 * @param {string} s
 * @param {string[]} wordDict
 * @return {boolean}
 */
const wordBreak = (s, wordDict) => {
  const input = s.split("");

  console.log("input", input);
};

export { wordBreak };
